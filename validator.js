function checkNewstitle(value)
    {
        return RegExp().test(value)
    }

module.exports = {
    checkNewstitle
}

function checkContactusqemail(value)
    {
        return RegExp().test(value)
    }

module.exports = {
    checkContactusqemail
}